# Preserve byte-exact source CSVs for bundle SHA-256 verification on every host.
# Python's csv writer emits CRLF; a carriage return at EOL is not stray whitespace.
*.csv -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
