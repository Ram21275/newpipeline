# Final results status

## Completion state

No new official-test GPU result is reported in this local delivery. The executable package, fresh-session notebook, protocol lock, split audit, paired polarity analysis, clustered bootstrap, final-logit agreement check, consistent matched-control constructor, Qwen3/LLaVA loaders, restricted DoLa baseline, resumable shards, and portable archive tooling are implemented and locally tested.

Still unexecuted on Kaggle:

- Real-model training-only smoke checks for the two locked checkpoints.
- Automatic final protocol lock using those smoke reports.
- LLaVA official-test final runs.
- Qwen3 official-test final runs.
- Frozen-feature probes, model-specific hidden-state interventions, and the shared re-encoding experiment at final scale.
- Clustered analysis, final figures, and result-bearing manuscript revisions from those new outputs.

## Historical evidence (not final-test evidence)

The prior paper reports development-only findings on 20 species and 26 attributes, including high species-probe accessibility, differing selector localization, and mostly null-compatible selective replacement effects. The corrected retained analysis already separates raw yes-minus-no margins from correctness-signed margins. These historical numbers remain context; they are not targets and are not relabeled as final results.

## New implementation evidence

The local synthetic/CPU suite passes. This establishes code-path behavior, not scientific outcomes. In particular, tests verify polarity signs, invalid-answer retention, non-fallback control matching, configuration-sensitive resumption, image-cluster resampling, restricted-DoLa labeling, and fail-closed protocol locking.

